const db = require('../database/db');

class Aluno {
  static cadastrarAluno(alunoData, callback) {
    // Implemente a lógica para inserir um aluno no banco de dados
  }

  static encontrarAlunoPorMatricula(matricula, callback) {
    // Implemente a lógica para encontrar um aluno por matrícula
  }
}

module.exports = Aluno;
